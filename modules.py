#top of document
#Note - this is not the same as file, ~/home/modules.py.
import json          #  built-in method;  modules.json.dumps(var);  modules.json.loads(var)
import re            #  built-in method;  RegEx;  reference - https://www.w3schools.com/python/python_regex.asp
import pyperclip     #  built-in method;  modules.pyperclip.copy();  var_copied = modules.pyperclip.paste()
import random        #  built-in method
import time          #  built-in method
import math          #  built-in method;  see also:  math_example.py;  modules.math.ceil(5.49);  result is 6, rounded up as ceiling function "ceil()"
import write_to_file
